# BrainBot Demo Environment Configuration
# Copy this file to .env in the project root and adjust as needed

# RSS Feed Configuration
RSS_FEED_PRESET=st          # Options: st, tc, ml, hf, ac, or full URL

# ChromaDB Configuration
CHROMA_HOST=localhost
CHROMA_PORT=8000
CHROMA_COLLECTION=brainbot_articles

# Deduplication Configuration
# Set to 'true' to mock deduplication (useful for demo without API keys)
# When enabled, all articles will be marked as NEW without checking ChromaDB
MOCK_DEDUPLICATION=true

# Generation Service Configuration
# Port 8002 is used to avoid conflict with ChromaDB on port 8000
GENERATION_SERVICE_URL=http://localhost:8002

# Webhook Configuration  
# Port for the demo's webhook server to listen on
WEBHOOK_PORT=9999
# The webhook URL is automatically constructed as http://localhost:${WEBHOOK_PORT}/webhook
# and sent to the generation service with each request

# Generation Service Settings (for generation_service/.env)
# HOST=0.0.0.0
# PORT=8002
# LOG_LEVEL=INFO
# GOOGLE_API_KEY=your_gemini_api_key_here
# FAL_KEY=your_fal_key_here
